import { ifOdd } from 'apem/helpers/if-odd';
import { module, test } from 'qunit';

module('Unit | Helper | if odd');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = ifOdd([42]);
  assert.ok(result);
});
