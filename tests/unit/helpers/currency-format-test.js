import { currencyFormat } from 'apem/helpers/currency-format';
import { module, test } from 'qunit';

module('Unit | Helper | currency format');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = currencyFormat([42]);
  assert.ok(result);
});
